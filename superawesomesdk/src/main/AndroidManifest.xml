<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="tv.superawesome.superawesomesdk" >

    <application
        android:allowBackup="true"
        android:label="@string/app_name" >
        <activity
            android:name="org.nexage.sourcekit.mraid.MRAIDBrowser"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen" />
        <activity
            android:name=".activities.SAVideoActivity"
            android:label="@string/title_activity_savideo" >
        </activity>
        <activity
            android:name=".activities.SAGamewallActivity"
            android:label="@string/title_activity_sagamewall" >

            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data android:scheme="bee7superawesomedemo" android:host="publisher"/>
            </intent-filter>

        </activity>
    </application>

</manifest>
